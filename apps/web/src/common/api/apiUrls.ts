export const API_BASE_URL = '' //서버 주소

export const API_URLS = {
  urls: {
    read: `${API_BASE_URL}/urls`,
    create: `${API_BASE_URL}/urls/create`,
    update: `${API_BASE_URL}/urls/update`,
    delete: `${API_BASE_URL}/urls/delete`
  }
}